apiVersion: v1
kind: Secret
metadata:
  name: comforthotel-secrets
  namespace: comforthotel
type: Opaque
stringData: {}
# Populate this Secret out-of-band before deployment.
# Required keys:
# - DB_PASSWORD
# - SESSION_SECRET
# - ADMIN_PASSWORD
# - MANAGER_PASSWORD
# - USER_PASSWORD
#
# The ghcr-pull-secret imagePullSecret is created by the deploy workflow
# from GitHub Secrets GHCR_USERNAME and GHCR_TOKEN.
